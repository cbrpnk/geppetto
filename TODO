Implement Specular lighting in the vertex shader
Add color / Texture to objects
use strtok to tokenize obj line
Make the mesh component have a static cache of every object it has seen
and make the object reference the appropriate VBO

